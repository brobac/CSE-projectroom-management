import { Modal } from "./Modal";

export const ReservationConfirmResultModal = () => {
  return (
    <Modal type="modal-reservation-confirm-result" closeButton>
      <div>예약확인 API 호출 결과를 띄울 모달</div>
    </Modal>
  );
};
